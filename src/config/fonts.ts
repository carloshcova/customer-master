/**
 * Self-hosted Lato font (design-system typeface) via @fontsource. Only the two
 * weights the DS defines are loaded: 400 (regular) and 700 (bold). Imported once
 * from the app provider so it loads both standalone and when the host mounts the
 * exposed CustomerApp.
 */
import '@fontsource/lato/400.css';
import '@fontsource/lato/700.css';
