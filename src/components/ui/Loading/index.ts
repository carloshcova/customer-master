/**
 * Loading primitives — MUI progress indicators, already on-brand (color
 * `primary`) via the theme. Re-exported under DS names; nothing to customize.
 *
 * - `Spinner`     — circular, indeterminate/determinate.
 * - `ProgressBar` — linear, indeterminate/determinate.
 */

export type {
  CircularProgressProps as SpinnerProps,
  LinearProgressProps as ProgressBarProps,
} from '@mui/material';
export {
  CircularProgress as Spinner,
  LinearProgress as ProgressBar,
} from '@mui/material';
