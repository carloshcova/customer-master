/**
 * Table primitives — MUI table parts, themed for the DS (header tinted with
 * `fbc-ice-01`, row separators `neutral-03`; see `theme.ts`). Compose these to
 * build any table (simple, with status Badges, action IconButtons, inline-edit
 * TextFields, …). Wrap in `TableContainer` and add sorting via `TableSortLabel`.
 */

export type {
  TableBodyProps,
  TableCellProps,
  TableContainerProps,
  TableFooterProps,
  TableHeadProps,
  TableProps,
  TableRowProps,
  TableSortLabelProps,
} from '@mui/material';
export {
  Table,
  TableBody,
  TableCell,
  TableContainer,
  TableFooter,
  TableHead,
  TableRow,
  TableSortLabel,
} from '@mui/material';
