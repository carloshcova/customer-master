# Public, client-exposed config. Only PUBLIC_*-prefixed vars are inlined into the
# client bundle by Rsbuild. NEVER put secrets here.
# Copy to `.env.local` for local overrides (gitignored).

# Base URL for the customer API (axios baseURL). Defaults to /api when unset.
PUBLIC_API_BASE_URL=/api
